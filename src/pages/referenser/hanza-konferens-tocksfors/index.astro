---
import { getEntry } from "astro:content";
import ReferencePage from "../../../components/references/ReferencePage.astro";
import "../../../styles/avab.css";

const entry = await getEntry("references", "hanza-konferens-tocksfors");

if (!entry) {
  throw new Error("Missing reference content: hanza-konferens-tocksfors");
}
---

<ReferencePage reference={entry.data} />
