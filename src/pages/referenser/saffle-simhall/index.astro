---
import { getEntry } from "astro:content";
import ReferencePage from "../../../components/references/ReferencePage.astro";
import "../../../styles/avab.css";

const entry = await getEntry("references", "saffle-simhall");

if (!entry) {
  throw new Error("Missing reference content: saffle-simhall");
}
---

<ReferencePage reference={entry.data} />
